import 'package:flutter/material.dart';

class InteractiveScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Interactive Exercises'),
      ),
      body: Center(
        child: Text('Interactive exercises content goes here.'),
      ),
    );
  }
}
